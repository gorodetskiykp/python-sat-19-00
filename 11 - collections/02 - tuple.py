# tuple, tuple()
# const = tuple()
# const = ()
# (1, 2, "hello", (1, 2))
# 1, 2, "hello", (1, 2)
# (1) - int
# (1, ) - tuple
# 1, - tuple

print(dir(tuple))

data = (1, 2, "hello", (1, 2))
print(data.count(2))  # 1
print(data.index("hello"))  # 2
